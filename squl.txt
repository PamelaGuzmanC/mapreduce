# Exercise 1
SELECT category, SUM(sales) 
FROM purchases 
GROUP BY category;

# Exercise 2
SELECT category, COUNT(*) 
FROM purchases 
GROUP BY category;

# Exercise 3
.

# Exercise 4
SELECT category, SUM(sales) 
FROM purchases 
WHERE category IN ('Computers', 'Cameras', 'Video Games')
GROUP BY category;
